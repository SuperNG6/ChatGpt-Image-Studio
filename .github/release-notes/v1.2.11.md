## 更新内容

- 新增宫格图切分工具，支持 `列*行` 输入、常用预设、手动添加横向/纵向切分线、拖拽调整和记忆切分配置。
- 图片卡片和图片预览界面均可直接打开切分工具。
- 宫格切分导出改为单个 ZIP 文件，切片按行优先顺序编号，例如 `001-r01-c01.png`。
- 更新前端 `axios` 到 `1.16.0`，修复生产依赖安全告警。

## 验证

- 前端 lint、类型检查、测试和构建通过。
- CI、Docker、Build Verify 工作流通过。
